import { createAction } from "utils/createAction"

export const selectHighlightElem = createAction('SELECT_HIGHLIGHT_ELEM')
export const deselectHighlightElem = createAction('DESELECT_HIGHLIGHT_ELEM')
